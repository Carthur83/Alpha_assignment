@model MemberLogInForm

@{
    Layout = "_LogInLayout";
    ViewData["Title"] = "Login";
}

<div class="center-screen">
    <div id="login" class="form-container">
        <h1>Login</h1>
        <form method="post" asp-controller="Auth" asp-action="Login" novalidate>
            <div class="signin-google">
                <button class="btn btn-google w-100"><img class="icon" src="~/images/google-icon.svg" alt="">Login with Google</button>
                <div class="divider">
                    <span>OR</span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="Email" class="form-label"></label>
                <input asp-for="Email" class="form-control">
                <span></span>
            </div>
            <div class="form-group">
                <label asp-for="Password" class="form-label"></label>
                <input asp-for="Password" class="form-control">
                <span></span>
            </div>
            <div class="form-check">
                <input class="form-check-input" type="checkbox" value="" id="flexCheckDefault">
                <label class="form-check-label" for="flexCheckDefault">
                    Remember Me
                </label>
                <span>
                    <a href="">Forgot Password?</a>
                </span>
            </div>
            <div class="form-group">
                <span class="error-message">
                    @ViewBag.ErrorMessage
                </span>
            </div>
            <button type="submit" class="btn btn-accent mt-1">Log In</button>
        </form>

        <p>Don't have an account? <a asp-controller="Auth" asp-action="CreateAccount">Sign Up</a></p>
    </div>

</div>